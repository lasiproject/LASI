@model LASI.WebApp.Models.AccountModel
@{
    ViewBag.Title = "Welcome " + Model.FirstName ?? string.Empty;
}
@functions{
    public string GetAntiForgeryToken() {
        string cookieToken, formToken;
        AntiForgery.GetTokens(null, out cookieToken, out formToken);
        return cookieToken + ":" + formToken;
    }
}
@using System.Web.Http;
@using LASI.WebApp.Models;
<header>
    <h1>@ViewBag.Title</h1>
</header>

<div id="main-content">

    <div>
        @{var uploadModel = new FileUploadModel { AcceptedExtensions = new[] { "*.txt" }, UploadTarget = "fileuploadtarget", };
            Html.RenderPartial("_DocumentUpload", uploadModel);
        }
    </div>
</div>
@section scripts {
    @Scripts.Render("~/Scripts/App/Home/Initialize.js")
    @Scripts.Render("~/Scripts/App/Home/Progress.js")
    @Scripts.Render("~/Scripts/App/Home/Index.js")


}





