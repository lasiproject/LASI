@model LASI.WebService.Models.LoginModel
@{
    ViewBag.Title = "Welcome";
}
@functions{
    public string GetAntiForgeryToken() {
        string cookieToken, formToken;
        AntiForgery.GetTokens(null, out cookieToken, out formToken);
        return cookieToken + ":" + formToken;
    }
}
@using System.Net.Http;
@using System.Web.Http;


<header>
    <h1>@ViewBag.Title</h1>
</header>

<div id="main-content">
    <div class="progress">
        <div id="progress1" class="progress-bar" role="progressbar" aria-valuenow="60" aria-valuemin="0" aria-valuemax="100" style="width: 0%; max-width:100%;">

        </div>
    </div>

    <div>
        @* @using (Html.BeginForm(new { action = "Home/Upload" })) {*@

        @FileUpload.GetHtml(name: "upload1", initialNumberOfFiles: 0, includeFormTag: true, addText: "add a file", uploadText: "upload")

        @*@using (Html.BeginForm("", "",
            new
            {
                enctype = "multipart/form-data",
                action = "/Home/Upload",
                UpdateElementId = "progress1"
            })) {


                <input id="file" type="file" name="file" multiple />
                <input type="submit" id="Submit" name="Submit" value="Upload" />


            }*@
    </div>



    @*<div>
            @{
                Html.RenderPartial("PartialPage");
            }
        </div>*@

</div>

@section scripts {

    @Scripts.Render("~/bundles/knockout")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jquery")
    @*@Scripts.Render("~/bundles/todo")*@
    @Scripts.Render("~/Scripts/app/Index/Index.js")
}





