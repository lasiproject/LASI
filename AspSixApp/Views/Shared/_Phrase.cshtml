@model PhraseModel
<span class=@Model.Style.CssClass
      title='@Model.DetailText'
      id=@Model.Id>
    @Model.Text
    <span id=@("context" + Model.Id) hidden>
        @Model.ContextMenuJson
    </span>
</span>
<!--the title attribute must be quoted for the DetailText property to display properly.-->